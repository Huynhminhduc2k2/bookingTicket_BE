const paymentMethod = {
    CREDIT_CARD: "credit_card",
    PAYPAL: "paypal",
    STRIPE: "stripe",
};

const paymentStatus = {
    PENDING: "pending",
    SUCCESS: "success",
    FAILED: "failed",
};

const paymentGateway = {
    PAYPAL: "paypal",
    STRIPE: "stripe",
};

export { paymentMethod, paymentStatus, paymentGateway };